# Build output
/target/
**/target/

# Backups / editor noise
*.rs.bk
*.swp
.vscode/
.idea/
.DS_Store

# QEMU / image artifacts
qemu.log
*.img
*.iso

# NOTE: Cargo.lock is intentionally NOT ignored — this is an OS,
# reproducible builds matter. Keep the lockfile committed.
