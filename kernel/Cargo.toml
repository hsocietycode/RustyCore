[package]
name = "rustycore-kernel"
version = "0.1.0"
edition = "2021"

[features]
# NOTE: default is alloc-bump — alloc-buddy doesn't exist yet and the
# kernel fails the build loudly if it is selected (see memory::init).
default = ["arch-x86_64", "sched-rr", "alloc-bump", "drv-uart"]
arch-x86_64 = []
sched-rr = []
sched-cfs = []
alloc-bump = []
alloc-buddy = []
drv-uart = []
drv-virtio = []
opt-speed = []
opt-size = []

[dependencies]
# Split of bootloader 0.11, done right:
# - kernel (this crate, bare-metal) links ONLY bootloader_api:
#   pure no_std, just the entry_point! macro + BootInfo types.
# - the host-side image builder (bootloader crate with std, BIOS/UEFI
#   disk image assembly) moves to xtask, which builds for the HOST
#   triple with full std. See xtask/Cargo.toml.
bootloader_api = "0.11"
x86_64 = "0.15"
spin = "0.10"
pic8259 = "0.11"
linked_list_allocator = "0.10"
